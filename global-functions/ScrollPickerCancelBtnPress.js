import * as ScrollPicker from '../custom-files/ScrollPicker';

const ScrollPickerCancelBtnPress = setGlobalVariableValue => {
  ScrollPicker.callCancel(setGlobalVariableValue);
};

export default ScrollPickerCancelBtnPress;
