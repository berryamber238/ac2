import * as ScrollPicker from '../custom-files/ScrollPicker';

const ScrollPickerConfirmBtnPress = Variables => {
  ScrollPicker.callConfirmCallback(Variables);
};

export default ScrollPickerConfirmBtnPress;
